@model IEnumerable<FinalGroupProjectTeam8.Models.Dispute>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Transaction.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comments)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DisputeType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CorrentAmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Transaction.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comments)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DisputeType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CorrentAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DisputeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DisputeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DisputeID })
        </td>
    </tr>
}

</table>
